/* prepend new meeting into meetings index page */
var first_meeting = $('.row.meeting.form').next()

$('<%=j(render(@meeting)) %>').insertBefore(first_meeting);
$('.row.meeting.form #new_meeting')[0].reset()